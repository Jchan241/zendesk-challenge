<div class="ticket-wrapper">
  <div class="container my-3">
    <h1>tickets</h1>

    <% @tickets.each do |ticket| %>
      <p><%= ticket.subject %></p>
      <%= link_to "here", ticket_path(ticket.id) %>
    <% end %>

    <%= will_paginate @tickets %>
  </div>
</div>
